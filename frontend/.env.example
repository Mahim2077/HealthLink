# Browser requests stay same-origin in local development and production.
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Optional server-only override for the local Next.js development proxy.
HEALTHLINK_BACKEND_ORIGIN=http://127.0.0.1:8000
